import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.phefvivianamardones',
  appName: 'phefvivianamardones',
  webDir: 'www'
};

export default config;
